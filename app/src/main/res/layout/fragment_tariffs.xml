<?xml version="1.0" encoding="utf-8"?>
<!-- LvovFlow: Server-Driven UI — tariffs loaded from WebView -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A1628">

    <WebView
        android:id="@+id/webview_tariffs"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#0A1628" />

    <!-- Loading spinner while page loads -->
    <ProgressBar
        android:id="@+id/tariffs_loading"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:indeterminateTint="#10B981" />

    <!-- Offline fallback -->
    <LinearLayout
        android:id="@+id/tariffs_offline"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        android:padding="32dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="😕"
            android:textSize="48sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Нет подключения к интернету"
            android:textColor="#94A3B8"
            android:textSize="16sp"
            android:textAlignment="center" />

        <Button
            android:id="@+id/btn_retry_tariffs"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginTop="16dp"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:text="Повторить"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textAllCaps="false"
            android:backgroundTint="#10B981"
            style="@style/Widget.Material3.Button" />
    </LinearLayout>

</FrameLayout>
